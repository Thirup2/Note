## 一、内容

```
+		// 一元正号
-		// 一元负号

*		// 乘法
/		// 除法
%		// 求模

+		// 加法
-		// 减法
```



## 二、基本属性

**结合律**：以上所有运算符都满足左结合律

**值类型**：算术运算符的运算对象和求值结果都是右值



## 三、注意事项

- `%`运算符只能运算两个整数之间的取余操作。对于`m % n`，如果其结果不为 0，则该结果的符号和`m`相同。
- 当`/`运算符作用于两个整数时，如果结果有小数部分，将会被丢弃，只保留整数部分。