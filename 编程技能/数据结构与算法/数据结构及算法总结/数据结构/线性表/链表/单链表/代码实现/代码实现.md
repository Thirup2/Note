```c
typedef int ElemType;      // 一个数据元素
struct node
{
    ElemType data;
    struct Node *next;
}
typedef struct node Node;  // 一个结点
typedef Node *PNode;       // 一个指向结点的指针
typedef Node *LinkList;    // 头指针兼链表
```